<tables>
{% for df in context.dfs %}
{% include 'shared/dataframe.tmpl' with context %}
{% endfor %}
</tables>

You are already provided with the following functions that you can call:
<function>
def execute_sql_query(sql_query: str) -> pd.Dataframe
    """This method connects to the database, executes the sql query and returns the dataframe"""
</function>

{% if last_code_generated != "" and context.memory.count() > 0 %}
{{ last_code_generated }}
{% else %}
Update this initial code:
```python
# TODO: import the required dependencies
    import pandas as pd
# Write code here

# A result array variable must be declared to store each generated code unit dictionary, for example:

   result=[code_unit_dict_1, code_unit_dict_2, ...] #Very important: The variable name must be "result", otherwise an error will occur.
   
   Please refer to the #Note section for instructions on how to generate the code_unit_dict.
```
{% endif %}
{% include 'shared/vectordb_docs.tmpl' with context %}
{{ context.memory.get_last_message() }}{{context.note}}

{% include 'shared/note.tmpl' with context %}