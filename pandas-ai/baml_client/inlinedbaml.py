###############################################################################
#
#  Welcome to Baml! To use this generated code, please run the following:
#
#  $ pip install baml-py
#
###############################################################################

# This file was generated by BAML: please do not edit it. Instead, edit the
# BAML files and re-generate this code.
#
# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

file_map = {
    
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\CoordinatorAgent.baml": "class Agents{\n    chatAgent ChatAgent[] @description(#\"\n    推荐的Agent集合，没有合适的则设置为空\n  \"#)\n    summary string @description(#\"\n    总结推荐的信息\n  \"#)\n} \nclass ChatAgent {\n  name string @description(#\"\n    Agent名称\n  \"#)\n  question string @description(#\"\n    对应的问题\n  \"#)\n  reason string @description(#\"\n    推荐的理由\n  \"#)\n  dependent string @description(#\"\n    需要依赖哪个Agent返回的数据，设置Agent的名称\n  \"#)\n}\n\nfunction Recommendation(user_query:string,agent_info: string) -> Agents {\n  client DeepSeekV3\n  prompt #\"    \n    你是一个专业的工作调度员，你拥有一个Agent团队，每个Agent都有自己的职责和技能。\n    你很擅长对用户的问题进行分解并推荐合适的Agent解答用户的问题，Agent的职责和技能描述是你推荐的重要依据。\n    如果用户的问题和Agent的职责和技能关系不大，则返回空chatAgent集合,只需要在summary返回信息即可。\n    如果用户问到你的团队信息，那么不必返回chatAgent集合，只需要在summary返回信息即可。\n        \n    Agent的信息:\n    ---\n    {{agent_info}}\n    ---\n\n    {{ _.role(\"user\") }}\n    QUESTION: {{ user_query }}\n    {{ ctx.output_format }}\n  \"#\n}\n\ntest RecommendAgent {\n  functions [Recommendation]\n  args {\n    agent_info #\"\n      ---Agent1---\n      name: 缺陷分析助手\n      skill: 分析缺陷数据\n\n      ---Agent2---\n      name: 员工信息助手\n      skill: 查询员工的详细信息\n    \"#\n    user_query #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息\n    \"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\EventAll.baml": "class EventAll{\n  eventKS EventKS \n  eventSB EventSB \n  eventTY EventTY\n}\n\n\n\n\nfunction GenEventAll(json:string) -> EventAll { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestGenEventAll {\n  functions [GenEventAll]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\EventKS.baml": "class EventKS{\n  extract_type string @description(#\"\n    设置为'#extract_type'的值\n  \"#)  \n  task_id string @description(#\"\n    案例id，从$task_id获取\n  \"#) \n\n  netEvent NetEvent[]  @description(#\"\n    网络事件实体集合\n  \"#)    \n  location Location[] @description(#\"\n    IP地址或小区实体集合\n  \"#) \n  links NetEventAndLocationRel[] @description(#\"\n    抽取2种关系：\n    1、网络事件实体和小区实体关系，从$results[*].data[*].row[*]..快速定界[*].conclusion里面抽取，查找'小区：'后面小区名称建立关系\n    2、网络事件实体和IP地址实体关系，从$results[*].data[*].row[*]..快速定界[*].conclusion里面抽取，查找'IP：'后面IP地址建立关系\n  \"#) \n}\n\nclass NetEvent {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何小区实体和IP实体id重复\n  \"#)  \n  n string @description(#\"\"\"\n      网络事件实体名称，如果名称相同则为同一实体，这点非常重要，从$data.outputs.report.results[*].data[*].row[*]..快速定界[*].sceneName获取\n  \"\"\"#) \n  @@stream.done\n}\n// 位置\nclass Location {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何网络事件实体id重复\n  \"#)    \n  t string @description(#\"\n    位置类型，如果抽取的是小区名称，设置为1，如果抽取的是IP地址，设置为2\n  \"#)\n  n string @description(#\"\"\"\n        小区实体名称，如果名称相同则为同一实体，这点非常重要，从$results[*].data[*].row[*]..快速定界[*].conclusion里面抽取，查找'小区：'后面地址类信息，例如：\n           '小区：佛山南海大沥滘口东区DC-ZFH-1 9次、佛山南海大沥滘口东区DC-ZFH-2 5次、佛山南海大沥上漖村南新区南二巷DC-KFH-1 3次；'\n           这里面'佛山南海大沥滘口东区DC-ZFH-1' 和 '佛山南海大沥滘口东区DC-ZFH-2' 和 '佛山南海大沥上漖村南新区南二巷DC-KFH-1'就是小区实体名称\n        IP地址，如果IP相同则为同一IP地址实体，这点非常重要，从$results[*].data[*].row[*]..conclusion里面抽取，查找'IP：'后面地址类信息，例如：\n           'IP：59.82.17.33 2次，2403:300:A0A:F000:0:0:0:7 3次'\n           这里面'59.82.17.33'和'2403:300:A42:C0A:0:0:0:8'就是IP地址\n  \"\"\"#) \n  @@stream.done\n}\n\n\n// 事件时间切片\nclass NetEventAndLocationRel {\n  s int @description(#\"\n    头部节点,设置为网络事件实体id\n  \"#)\n  t int @description(#\"\n    尾部节点id,设置为小区实体或IP地址实体id\n  \"#)   \n  c int @description(#\"\n    事件发生的次数，从$results[*].data[*].row[*]..快速定界[*].conclusion获取\n  \"#) \n  r string @description(#\"\n    事件原因归类，从$results[*].data[*].row[*]..快速定界[*].causeType获取\n  \"#) \n  d string @description(#\"\n    事件发生日期区间，从$results[*].data[*].row[*]..快速定界[*].conclusion获取时间区间，例如:\n    \"频繁互操作：2025-04-06 01:23:44.502 - 2025-04-06 01:24:15.508，发生频繁互操作5次。小区：惠州西区亲水湾【西区邮政2】D-HLW-1、未知小区51626057729\"\n    这里面'2025-04-06 01:23:44.502 - 2025-04-06 01:24:15.508'就是事件发生日期区间\n  \"#)   \n  @@stream.done\n}\n\n\n\nfunction GenEventKS(json:string,extract_type:string) -> EventKS { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    要注意，以下几点非常重要，不遵循会导致致命错误：\n    1、所有实体的id必须全局唯一\n    2、遇到相同事件合并为一个网络事件实体\n    3、遇到相同小区合并为一个小区实体\n    4、遇到相同IP合并为一个IP实体\n    #extract_type:{{extract_type}}    \n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestGenEventKS {\n  functions [GenEventKS]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n    extract_type #\"netEvent\"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\EventSB.baml": "class EventSB{\n\n  extract_type string @description(#\"\n    设置为'#extract_type'的值\n  \"#)  \n  task_id string @description(#\"\n    案例id，从$task_id获取\n  \"#) \n\n  netEventFail NetEventFail[]  @description(#\"\n    网络事件实体集合\n  \"#)    \n  locationFail LocationFail[] @description(#\"\n    IP地址或小区实体集合\n  \"#) \n  links EventFailAndLocationRel[] @description(#\"\n    抽取2种关系：\n    1、网络事件实体和小区实体关系，从$results[*].data[*].row[*]..失败分析定界[*].cell获取小区名称建立关系\n    2、网络事件实体和IP地址实体关系，从$results[*].data[*].row[*]..失败分析定界[*].ip里面获取IP地址建立关系\n  \"#) \n\n}\n\nclass NetEventFail {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何小区实体和IP实体id重复\n  \"#)  \n  n string @description(#\"\"\"\n      网络事件实体名称，如果名称相同则为同一实体，这点非常重要，从$results[*].data[*].row[*]..失败分析定界[*].procedureType获取\n  \"\"\"#) \n  @@stream.done  \n\n}\n// 位置\nclass LocationFail {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何网络事件实体id重复\n  \"#)    \n  t string @description(#\"\n    位置类型，如果抽取的是小区名称，设置为1，如果抽取的是IP地址，设置为2\n  \"#)\n  n string @description(#\"\"\"\n    小区实体名称，如果名称相同则为同一实体，这点非常重要，从从$results[*].data[*].row[*]..失败分析定界[*].cell里面抽取,为空则忽略该小区实体\n    IP地址，如果IP相同则为同一IP地址实体，这点非常重要，从$results[*].data[*].row[*]..失败分析定界[*].ip里面抽取，为空则忽略该IP实体\n  \"\"\"#) \n  @@stream.done  \n}\n\n\n// 事件时间切片\nclass EventFailAndLocationRel {\n  s int @description(#\"\n    头部节点,设置为网络事件实体id\n  \"#)\n  t int @description(#\"\n    尾部节点id,设置为小区实体或IP地址实体id\n  \"#)   \n  c int @description(#\"\n    事件的发生次数，从$results[*].data[*].row[*]..失败分析定界[*]..count获取次数\n  \"#) \n  r string @description(#\"\n    事件原因归类，从$results[*].data[*].row[*]..失败分析定界[*].causeType获取\n  \"#) \n \n  @@stream.done  \n}\n\n\n\nfunction GenEventSB(json:string,extract_type:string) -> EventSB { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    要注意，以下几点非常重要，不遵循会导致致命错误：\n    1、所有实体的id必须全局唯一\n    2、遇到相同事件合并为一个网络事件实体\n    3、遇到相同小区合并为一个小区实体\n    4、遇到相同IP合并为一个IP实体\n    #extract_type:{{extract_type}}      \n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestEventSB {\n  functions [GenEventSB]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n    extract_type #\"netEventFail\"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\EventTY.baml": "class EventTY{\n  extract_type string @description(#\"\n    设置为'#extract_type'的值\n  \"#)  \n  task_id string @description(#\"\n    案例id，从$task_id获取\n  \"#) \n\n  netEventTy NetEventTy[]  @description(#\"\n    网络事件实体集合\n  \"#)    \n  locationTy LocationTy[] @description(#\"\n    IP地址或小区实体集合\n  \"#) \n  links EventAndLocationTyRel[] @description(#\"\n    抽取2种关系：\n    1、网络事件实体和小区实体关系，从$results[*].data[*].row[*]..通用定界[*].conclusion里面抽取，查找'小区：'后面小区名称建立关系\n    2、网络事件实体和IP地址实体关系，从$results[*].data[*].row[*]..通用定界[*].conclusion里面抽取，查找'IP：'后面IP地址建立关系\n  \"#) \n  \n}\n\nclass NetEventTy {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何小区实体和IP实体id重复\n  \"#)  \n  n string @description(#\"\"\"\n      网络事件实体名称，如果名称相同则合并为同一实体，这点非常重要，从$data.outputs.report.results[*].data[*].row[*]..通用定界[*].eventName获取\n  \"\"\"#) \n  @@stream.done\n}\n// 位置\nclass LocationTy {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何网络事件实体id重复\n  \"#)    \n  t string @description(#\"\n    位置类型，如果抽取的是小区名称，设置为1，如果抽取的是IP地址，设置为2\n  \"#)\n  n string @description(#\"\"\"\n        小区实体名称，如果名称相同则合并为同一实体，这点非常重要，从$results[*].data[*].row[*]..通用定界[*].conclusion里面抽取，查找'小区：'后面地址类信息，例如：\n           '小区：佛山南海大沥滘口东区DC-ZFH-1 9次'\n           这里面'佛山南海大沥滘口东区DC-ZFH-1' 就是小区实体名称，紧跟着的'9次'表示发生在该小区的网络事件次数\n        IP地址，如果IP相同则为合并为同一实体，这点非常重要，从$results[*].data[*].row[*]..通用定界[*].conclusion里面抽取，查找'IP：'后面地址类信息，例如：\n           'IP：59.82.17.33 2次'\n           这里面'59.82.17.33'就是IP地址，紧跟着的'2次'表示发生在该ip的网络事件次数\n  \"\"\"#) \n  @@stream.done\n}\n\n\n\nclass EventAndLocationTyRel {\n  s int @description(#\"\n    头部节点,设置为网络事件实体id\n  \"#)\n  t int @description(#\"\n    尾部节点id,设置为小区实体或IP地址实体id\n  \"#)   \n  c int @description(#\"\n    事件发生的总次数，从$results[*].data[*].row[*]..通用定界[*].count获取\n  \"#) \n  dc int @description(#\"\n    在小区或IP发生的次数，从$results[*].data[*].row[*]..通用定界[*].conclusion获取，数值紧跟在小区或IP地址的后面\n  \"#) \n  r string @description(#\"\n    事件原因归类，从$results[*].data[*].row[*]..通用定界[*].causeType获取\n  \"#) \n  @@stream.done\n}\n\n\n\nfunction GenEventTY(json:string,extract_type:string) -> EventTY { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    要注意，以下几点非常重要，不遵循会导致致命错误：\n    1、所有实体的id必须全局唯一，这点很重要。\n    2、遇到相同事件合并为一个网络事件实体\n    3、遇到相同小区合并为一个小区实体\n    4、遇到相同IP合并为一个IP实体\n    #extract_type:{{extract_type}}      \n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestGenEventTY {\n  functions [GenEventTY]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n    extract_type #\"NetEventTy\"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\GenPythonCode.baml": "class PythonCode {\n  code string @description(#\"\n    Used to store the generated Python code\n  \"#)\n}\n\nfunction GenPythonCode(prompt:string) -> PythonCode {\n  client DeepSeekV3\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    {{prompt}}\n\n    {{ ctx.output_format }}\n  \"#\n}\n\ntest TestGenPythonCode {\n  functions [GenPythonCode]\n  args {\n    prompt #\"\n      You already have access to the following dataset: \n    <tables>\n    \n    <table dialect=\"duckdb\" table_name=\"abug\" description=\"abug\" dimensions=\"342x7\">\n    <sample_data>\n    行号,缺陷状态,开发人员,概要,提出日期,关闭时间,异常原因\n    1,处理中,wangxiaofang,【bug】标注管理台->标注模板管理-定制化模板：【另存为】模板时，调错接口，并且用户信息为空,2025/3/3 16:31,,Code Error\n    2,已关闭,zhoujunwei,【bug】多模态语料库->纯数据集管理：数据集本来是公开状态，在编辑页面修改公开范围后进行审核通过，公开范围没有成功改变,2025/2/8 18:36,2025/2/12 11:15,Code Error\n    3,开放,linpeiliang, 【bug】标注管理台->智能标注模型管理：能力名称、类型需要与能力清单一致,2025/1/21 10:16,,Requirements\n    4,开放,zhoujunwei,【bug】多模态语料库->标注数据集管理：审核通过以后，纯数据集是能正常使用的，但是和纯数据关联的标注数据集无法显示,2025/2/8 18:28,,Code Error\n    5,已关闭,zhoujunwei,【bug】公共管理模块->FTP信息管理：条件筛选无效果；查询关键字报错,2025/2/21 16:22,2025/2/27 18:53,Code Error\n    </sample_data>\n    <columns description=\"Refer to the following field definition information when generating SQL.\">\n    <column name=\"行号\" type=\"integer\"/>\n    <column name=\"缺陷状态\" type=\"string\"/>\n    <column name=\"开发人员\" type=\"string\"/>\n    <column name=\"概要\" type=\"string\"/>\n    <column name=\"提出日期\" type=\"string\"/>\n    <column name=\"关闭时间\" type=\"string\"/>\n    <column name=\"异常原因\" type=\"string\"/>\n    </columns>\n    </table>\n    \n    \n    </tables>\n    \n    你可以使用以下工具:\n    - execute_sql_query(sql_query: str) -> pd.Dataframe\n        该工具连接数据，执行sql查询并返回pd.Dataframe\n    \n    Based on the dataset information and tools provided above, generate Python code for the USER QUERY.\n    If the user's question is not clear enough, do not forcibly generate SQL. Feedback information should be provided to the user, asking them to provide more details.\n    Before generating the code, refer to the #Note section for instructions on how to generate the code_unit_dict.\n    \n    \n    \n    ### USER QUERY\n     统计数据\n    \n    #Note：Below are the requirements for generating Python code, which must be followed:\n    - At the end, must declare \"result\" variable as a list to store each generated code unit dictionary, for example:\n    \n       result=[code_unit_dict_1, code_unit_dict_2, ...] #Very important: The variable name must be \"result\" and should not be preceded by a definition like \"def\", otherwise an error will occur.\n       \n    - Generate python code and return full updated code,code comments are written in Chinese.\n    - Important: When encountering complex problems, it is essential to first break down the problem into steps that involve obtaining simpler datasets. Then, merge and compute each step's data to derive the final result.\n    - Important: Do not create database connections and query methods on your own. You must use the execute_sql_query tool for SQL queries.\n    - When writing SQL,it is essential to strictly adhere to the database dialect to generate the correct SQL syntax, particularly focusing on the SQL functions used.\n    - When writing SQL,only relevant table for query and do aggregation, sorting, joins and grouby through sql query.\n    - When writing SQL, if you encounter an ORDER BY clause, do not enclose the fields in single quotes. For example, ORDER BY '年龄' is incorrect.\n    - When writing SQL, using two consecutive percent signs in the date formatting function is incorrect. For example, DATE_FORMAT(date_field, '%%Y-%%m') is wrong. The correct format should use single percent signs, such as '%Y-%m'.\n    - When writing SQL, the field names used in the ORDER BY clause should be consistent with the aliases defined in the SELECT portion.\n    - Since generating SQL statements frequently results in errors when involving date handling, it is essential to thoroughly study the content in the '#SQL Date Handling' section first.\n    - Important: The code for fetching data and generating visualization objects must be organized as a complete code unit. The code of one unit must be organized together,if there are multiple code units.Refer to the following steps to generate a unit of code:\n    -- Step1: Define a dictionary to hold code unit variables\n      This dict is used to store together all variables that a code unit intends to display,it must be a global variable. The dict should be named uniquely across the global scope. The schema for the dict is as follows:\n      code_unit_dict_1={\n        \"sql\":\"<SQL query to generate data>\",\n      \"sql_explain\":\"<Detailed explanation of the logic behind SQL statement generation>\",\n      \"question\":\"<Describe the user's question regarding this SQL query in a way that humans can understand>\",\n        \"title\":\"<The title should reflect the business significance of the data>\",\n        \"explanatory\":\"<the explanatory text should make it easy for humans to understand the business context of the data>\",\n        \"data\":<dataframe instance>,\n      \"nicegui_table_columns\":<nicegui_table columns information>,\n      \"nicegui_table_add_slots_info\":<A dictionary to store the parameters of add_slot, where the key is the value of the name parameter, and the value is the value of the template parameter>\n        \"chart\":<echart config>\n      }\n    \n    -- Step2: Obtain a DataFrame\n        First, you need to obtain a DataFrame by executing an SQL query. Here’s an example of how to do this:\n        code_unit_dict_1={}\n        df_1 = execute_sql_query(sql_query)\n        code_unit_dict_1[\"sql\"]=sql_query\n        code_unit_dict_1[\"sql_explain\"]=\"从员工表进行统计，通过count(员工ID)函数获取员工总数\"\n        code_unit_dict_1[\"data\"]=df_1\n        code_unit_dict_1[\"question\"]=\"统计员工总人数\"\n    \n    -- Step3: DataFrame会用表格进行展示，分析用户问题是否额外要求生成表格的可视化配置\n        必须通过学习'# 表格可视化配置'章节内容正确设置config_item_name和config值：\n        table_visualization_config1={\n          config_item_name1:config1,\n          config_item_name2:config2,\n          ...\n          ...\n        }\n        code_unit_dict_1[\"table_visualization_config\"]=table_visualization_config1\n    \n    -- Step4: Display titles and explanatory text  \n        The title should reflect the business significance of the data, and the explanatory text should make it easy for humans to understand the business context of the data.For example:\n        title_var_1='每个职员的订单数量统计'\n        explanatory_var_1='以下表格展示了每位员工在不同门店处理的订单数量。'\n        code_unit_dict_1[\"title\"]=title_var_1\n        code_unit_dict_1[\"explanatory\"]=explanatory_var_1\n    \n    -- Step5: 请判断用户是否有示绘制图表意图,如果有则按照以下规范生成图表的可视化配置,在生成可视化配置前必须先学习'#图表可视化配置'章节内容：\n        生成基本的配置即可,不要使用function来动态计算值：    \n        chart_visualization_config_1 = {\n          //x轴字段，如果图表类型不需要则不要生成，譬如饼图\n          'xAxis': <参考echart进行配置>,\n          //y轴字段，如果图表类型不需要则不要生成，譬如饼图,\n          'yAxis': <参考echart进行配置>,\n          'title': <图表标题,参考echart进行配置,放在左边>,\n          'legend':<图表标题,参考echart进行配置,放在右边>,\n          'data_item_mark_point':[<用户要求特殊标记数据项，具体配置参考'# 图表可视化配置章节'>,...],\n          //datasets表示一个数据集的可视化配置\n          'datasets': [\n            {\n              //分析用户问题，设置图表类别柱状图(bar) or 饼状图(pie) or 折线图(line) or 面积图(area) or 玫瑰图(rose) or 散点图(scatte)\n              \"type\":<bar or pie or line>,\n              //数据集\n              \"data\":<根据图表类别参考echart进行配置>\n            },\n            ...\n          ]\n    \n        }\n        ...\n        ...\n        code_unit_dict_1[\"chart\"]=[echart_config_1,...]\n    \n    \n    - If there are multiple code units, each should catch exceptions but avoid rethrowing them to prevent localized errors from halting the entire program. However, if there is only a single code unit, exception catching is unnecessary.For example:\n    \n      try:\n        unit_code_1\n      except Exception as e:\n        code_unit_dict_1[\"question\"]=\"统计每个职员的订单数量\"\n        print(f'Error in code_unit_1: {e}')\n      \n      try:\n        unit_code_2\n      except Exception as e:\n        code_unit_dict_2[\"question\"]=\"统计每个门店的订单数量\"\n        print(f'Error in code_unit_2: {e}')  \n      ...\n      ...\n    - \n    \n    - Do not output any Streamlit code inside the code.\n    - If the user does not specify otherwise, when dealing with decimals, retain two decimal places, such as 1.55. If it is a percentage or proportion, display it in the format of xx.xx%, for example, 20.12%.\n    \n    ###学习资料###\n    # 以下是用户的问题是否清晰，例如以下例子：\n    - 用户问:统计数量，但该提问没有明确统计什么数据，不要随意发挥给用户返回你认为的数据\n    # SQL Date Handling:\n    - 当db dialect是duckdb时：\n    -- 如果日期字段为字符串格式，那么必须先转换为日期格式：CAST(<日期字段> AS DATE)\n    -- 将日期转为'月'时使用strftime(<日期字段>,'%Y-%m')，如果日期字段为字符串，则需要先转换：strftime(CAST(<日期字段> AS DATE),'%Y-%m')\n    -- 将日期转为'日'时使用strftime(<日期字段>,'%Y-%m-%d')，如果日期字段为字符串，则需要先转换：strftime(CAST(<日期字段> AS DATE,'%Y-%m-%d'))\n    -- 计算昨天(1天前)使用CURRENT_DATE - INTERVAL '1 day'；计算前天(2天前)使用CURRENT_DATE - INTERVAL '2 day'\n    -- 计算N天前使用CURRENT_DATE - INTERVAL '<N> day'\n    # 表格可视化配置：\n    - 如果用户要求根据数据比较设置某一列表格单元格特殊展示样式，譬如年龄列大于10显示红色，配置项如下：\n      column_style_by_data_compare:{\n        \"field_name\":<数据字段名称，不能为空>,\n        \"comparison_operators\":'<比较符,包括大于(>)、小于(<)、等于(==)、最大值(max)、最小值(min)，不能为空>',\n        \"comparison_value\":<数据字段需要比较的值，当comparison_operators为max或min时设置为空，否则必须设置值>,\n        \"color\":'<需要设置的颜色值，不能为空>'\n      }\n    \n    # 图表可视化配置：\n    - 非常重要：在生成图表配置时不要使用function来动态计算值\n    - 如果用户要求设置数据项颜色(例如对于柱状图，要求每个柱子颜色不一样，对于折线图，要求每条线颜色不一样，其他图形类似)：\n      data_item_color_setting:<0 or 1，0表示要求每个数据项颜色一样，1表示要求每个数据项颜色不一样>\n    - 如果用户要求特殊标记数据项最大、最小值(例如对于柱状图，要求突出数值最高的柱子为红色等):\n      data_item_mark_point:{\n        /**\n          data是一个数组，用于定义需要标记的点，包括最大值(max)、最小值(min)、平均值(average)3种，根据问题要求可存在一个或多个，譬如同时标记最高点和最低点，标记点的schema如下：\n          {\n            //标记点类型，有最大值(max)、最小值(min)、平均值(average)三种\n            \"type\": '<max or min or average，不能为空>', \n            \"name\":'<最大值 or 最小值 or平均值，不能为空>',\n            //设置标记点的样式，例如颜色、边框等\n            color: '<颜色>',\n            size:<标记大小>,\n            //表示标记的形状\n            \"symbol\": <pin>\n          } \n        **/\n        \"data\":[\n            标记点1,\n            标记点2,\n            ...\n          ]\n      }\n    \"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\IndexCE.baml": "class IndexCE{\n  extract_type string @description(#\"\n    设置为'#extract_type'的值\n  \"#)\n  task_id string @description(#\"\n    案例id，从$task_id获取\n  \"#) \n  indexOfCell IndexOfCell[]  @description(#\"\n    指标实体集合\n  \"#)    \n  cell Cell[] @description(#\"\n    小区实体集合\n  \"#) \n  links IndexAndCellRel[] @description(#\"\n    指标实体和小区实体关系集合，从$data.outputs.report.results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'小区维度'开头的数组元素里面抽取，注意要在同一个数组元素里面判断指标实体和小区实体的关系\n  \"#)   \n\n}\n\nclass IndexOfCell {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何小区实体id重复\n  \"#)   \n  n string @description(#\"\n    指标实体名称，如果名称相同则为同一实体，这点非常重要，从$results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'小区维度'开头和包含'有集中性'的数组元素里面抽取，指标实体名称在'小区维度，指标：'后面，例如以下数组元素：\n    '小区维度，指标：TCP建链平均时延(ms)，无集中性，集中度为0.5000，低于阈值0.5000'\n    这里面'TCP建链平均时延(ms)'中'TCP建链平均时延'就是指标实体名称,最右边'(ms)'是单位，其他指标也一样，切记不要附加到指标名称后面\n  \"#) \n  @@stream.done\n}\nclass Cell {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何指标实体id重复\n  \"#)   \n  n string @description(#\"\n    小区实体名称，如果名称相同则为同一实体，这点非常重要，从$results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'小区维度'开头和包含'有集中性'的数组元素里面抽取，查找'、指标值'前面的小区实体名称，例如以下数组元素：\n    '小区维度，指标：附着/注册平均时延(ms)，有集中性，集中度为0.8000，高于阈值0.6000\\n\\t深圳松岗捷创D-ZRH-3、指标值：122.88, 总时延：3932.00, 成功次数：32.00, 影响度：-0.0956'\n    这里面'深圳松岗捷创D-ZRH-3'就是小区实体名称\n  \"#)  \n  @@stream.done\n}\n\nclass IndexAndCellRel {\n  s int @description(#\"\n    头部节点,设置为指标实体id\n  \"#)\n  t int @description(#\"\n    尾部节点id,设置为小区实体id\n  \"#)     \n  v string @description(#\"\n    指标值，$results[*].data[*].row[*]..用户指标集中性分析[*]..小区名称后面的'指标值:'里面抽取，例如：\n    '深圳松岗捷创D-ZRH-3、指标值：122.88, 总时延：3932.00, 成功次数：32.00, 影响度：-0.0956'\n    这里面122.88是指标值，3932.00是指标分子，32.00是指标分母\n  \"#)\n  j string @description(#\"\n    指标是否有集中性，$results[*].data[*].row[*]..用户指标集中性分析[*]..提取小区维度的指标有无集中性，'有集中性'设置为1，'无集中性'设置为0\n  \"#)    \n  z string @description(#\"\n    指标分子\n  \"#)   \n  m string @description(#\"\n    指标分母\n  \"#)   \n  @@stream.done\n}\n \n\n\nfunction GenIndexCE(json:string,extract_type:string) -> IndexCE { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    要注意，以下几点非常重要，不遵循会导致致命错误：\n    1、所有实体的id必须全局唯一\n    2、遇到相同指标合并为一个指标实体\n    3、遇到相同小区合并为一个小区实体\n\n    #extract_type:{{extract_type}}\n    {{IndexList()}}\n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestIndexCE {\n  functions [GenIndexCE]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n    extract_type #\"indexOfCell\"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\IndexGL.baml": "class IndexGL{\n  extract_type string @description(#\"\n    设置为'#extract_type'的值\n  \"#)  \n  task_id string @description(#\"\n    案例id，从$task_id获取\n  \"#) \n  indexOverview IndexOverview[] @description(#\"\n    指标实体，从$data[*]..指标概览详情[*]，name相同的为同一个指标实体\n  \"#)   \n  netOverviewNodes NetOverview[] @description(#\"\n    网络实体集合，从$data[*]..指标概览详情[*]和驻留网络信息[*].network里面抽取，network相同的为同一个网络实体\n  \"#) \n\n  links IndexGlAndNetRel[] @description(#\"\n    网络实体和指标实体关系集合，从$data[*]..指标概览详情[*]抽取，注意要在同一个元素里面判断网络实体和指标实体的关系\n  \"#) \n}\n\n\n// class IndexAndIndexTimeSliceRelation {\nclass IndexOverview {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何网络实体id重复\n  \"#)  \n  n string @description(#\"\n    指标实体名称，如果名称相同则为同一实体，这点非常重要，从$data[*]..指标概览详情[*].name和驻留网络信息[*]里面抽取，name相同的为同一个指标实体\n  \"#) \n  @@stream.done\n}\n// class Index {\nclass NetOverview {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何指标实体id重复\n  \"#)  \n  n string @description(#\"\n    网络实体名称，如果名称相同则为同一实体，这点非常重要，从$data[*]..指标概览详情[*]和驻留网络信息[*].network里面抽取，network相同的为同一个网络实体\n  \"#)  \n  @@stream.done\n  \n}\n\nclass IndexGlAndNetRel {\n  s int @description(#\"\n    头部节点,设置为指标id\n  \"#)\n  t int @description(#\"\n    尾部节点id,设置为网络实体id\n  \"#)\n  v float @description(#\"\n    指标值，从$data[*]..指标概览详情[*].value和驻留网络信息[*]里面抽取\n  \"#)\n  @@stream.done\n\n}\n \n\n\nfunction GenIndexGL(json:string,extract_type:string) -> IndexGL { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    要注意，以下几点非常重要，不遵循会导致致命错误：\n    1、所有实体的id必须全局唯一\n    2、遇到相同指标合并为一个指标实体\n    3、遇到相同网络合并为一个网络实体\n    #extract_type:{{extract_type}}\n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestGenIndexGL {\n  functions [GenIndexGL]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n    extract_type #\"indexOverview\"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\IndexGL4g.baml": "class IndexGL4g{\n  task_id string @description(#\"\n    案例id，从$task_id获取\n  \"#) \n\n  indexOverviewTriple4g IndexOverviewTriple4g[] @description(#\"\n    指标和指标网络类型的三元组集合，从以下地方抽取：\n    1、从$data[*]..指标概览详情[*]数组抽取多个指标实体,只抽取network是4G的实体\n    2、从$data[*]..驻留网络信息[*]数组抽取多个指标实体,只抽取network是4G的实体\n      驻留网络信息中指标输出使用中文名称，指标对应中文名如下：{\"dlData(MB)\":\"HTTP流量\", \"dlDataRatio(%)\":\"HTTP流量占比\",\"resideTimeRatio(%)\":\"驻留时长占比\",\"resideTime(s)\":\"驻留时长\"}\n  \"#)    \n}\n\n// class IndexAndIndexTimeSliceTriple {\nclass IndexOverviewTriple4g {\n  h IndexOverview4g  @description(#\"\n    头部节点,设置为指标实体\n  \"#) \n  t NetOverview4g  @description(#\"\n    尾部节点,设置网络实体\n  \"#) \n  r IndexGlAndNetRel4g @description(#\"\n      指标和指标网络的关系\n  \"#) \n  @@stream.done\n}\n// class IndexAndIndexTimeSliceRelation {\nclass IndexOverview4g{\n  tp string @description(#\"\n    设置为index\n  \"#)  \n  n string @description(#\"\n    指标名称，从$data[*]..指标概览详情[*].name和驻留网络信息[*]里面抽取\n  \"#) \n}\n// class Index {\nclass NetOverview4g {\n  tp string @description(#\"\n    设置为n\n  \"#)\n  n string @description(#\"\n    所属网络，从$data[*]..指标概览详情[*]和驻留网络信息[*]\n  \"#)  \n  \n}\n\nclass IndexGlAndNetRel4g {\n  v string @description(#\"\n    指标值，从$data[*]..指标概览详情[*].value和驻留网络信息[*]里面抽取\n  \"#)\n  n string @description(#\"\n    设置值\"BELONG_TO\"，表示指标归属的网络\n  \"#) \n\n}\n \n\n\nfunction GenIndexGL4g(json:string) -> IndexGL4g { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestGenIndexGL4g {\n  functions [GenIndexGL]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\IndexNU.baml": "class IndexNU{\n  extract_type string @description(#\"\n    设置为'#extract_type'的值\n  \"#)\n  task_id string @description(#\"\n    案例id，从$task_id获取\n  \"#) \n\n  indexNetUnit IndexNetUnit[]  @description(#\"\n    指标实体集合\n  \"#)    \n  netUnit NetUnit[] @description(#\"\n    网元实体集合\n  \"#) \n  links IndexAndNetUnitRel[] @description(#\"\n    指标实体和网元实体关系集合，从$data.outputs.report.results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'网元维度'开头的数组元素里面抽取，注意要在同一个数组元素里面判断指标实体和网元实体的关系\n  \"#)  \n \n}\n\nclass IndexNetUnit {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何网元实体id重复\n  \"#)    \n  n string @description(#\"\n    指标实体名称，如果名称相同则为同一实体，这点非常重要，从$results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'网元维度'开头和包含'有集中性'的数组元素里面抽取，指标实体名称在'网元维度，指标：'后面，例如以下数组元素：\n    '网元维度，指标：上行乱序率(HTTPS)(%)，有集中性，集中度为0.7500，高于阈值0.5000'\n    这里面'上行乱序率(HTTPS)(%)'中'上行乱序率(HTTPS)'就是指标实体名称,最右边'(%)'是单位，其他指标也一样，切记不要附加到指标名称后面\n  \"#) \n  @@stream.done\n}\nclass NetUnit {\n\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何指标实体id重复\n  \"#)   \n  n string @description(#\"\n    网元实体名称，如果名称相同则为同一实体，这点非常重要，从$results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'网元维度'开头和包含'有集中性'的数组元素里面抽取，查找'、指标值'前面的网元实体名称，例如以下数组元素：\n     '网元维度，指标：上行乱序率(HTTPS)(%)，有集中性，集中度为0.7500，高于阈值0.5000\\n\\tSZHUPF018BHW、指标值：5.22, 乱序次数：293.00, 总次数：5613.00, 影响度：-0.4229\\n\\tAPP-HNZJIgdAMF020BHW-02AHW012、指标值：85.71, 成功次数：24.00, 总次数：28.00'\n      这里面'SZHUPF018BHW','APP-HNZJIgdAMF020BHW-02AHW012'就是网元实体名称\n  \"#)  \n  @@stream.done\n}\n\nclass IndexAndNetUnitRel {\n  s int @description(#\"\n    头部节点,设置为指标实体id\n  \"#)\n  t int @description(#\"\n    尾部节点id,设置为网元实体id\n  \"#)    \n  v string @description(#\"\n    指标值，$results[*].data[*].row[*]..用户指标集中性分析[*]..网元维度名称后面的'指标值:'里面抽取，例如：\n    'SZHUPF018BHW、指标值：5.22, 乱序次数：293.00, 总次数：5613.00, 影响度：-0.4229'\n    这里面5.22是指标值，293.00是指标分子，5613.00是指标分母\n  \"#)\n  j string @description(#\"\n    指标是否有集中性，$results[*].data[*].row[*]..用户指标集中性分析[*]..提取网元维度的指标有无集中性，'有集中性'设置为1，'无集中性'设置为0\n  \"#)    \n  z string @description(#\"\n    指标分子\n  \"#)   \n  m string @description(#\"\n    指标分母\n  \"#)   \n  @@stream.done\n}\n \n\n\nfunction GenIndexNU(json:string,extract_type:string) -> IndexNU { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    要注意，以下几点非常重要，不遵循会导致致命错误：\n    1、所有实体的id必须全局唯一\n    2、遇到相同指标合并为一个指标实体\n    3、遇到相同网元合并为一个网元实体\n\n    #extract_type:{{extract_type}}\n    {{IndexList()}}\n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestIndexNU {\n  functions [GenIndexNU]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n    extract_type #\"indexNetUnit\"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\IndexYW.baml": "class IndexYW{\n  extract_type string @description(#\"\n    设置为'#extract_type'的值\n  \"#)\n  task_id string @description(#\"\n    案例id，从$task_id获取\n  \"#) \n\n  indexOfBus IndexOfBus[]  @description(#\"\n    指标实体集合\n  \"#)    \n  bus Bus[] @description(#\"\n    业务小类实体集合\n  \"#) \n  links IndexAndBusRel[] @description(#\"\n    指标实体和业务小类实体关系集合，从$data.outputs.report.results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'业务小类维度'开头的数组元素里面抽取，注意要在同一个数组元素里面判断指标实体和业务小类实体的关系\n  \"#)   \n}\n \n\nclass IndexOfBus {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何业务实体id重复\n  \"#)  \n  n string @description(#\"\n    指标实体名称，如果名称相同则为同一实体，这点非常重要，从$data.outputs.report.results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'业务小类维度'开头和包含'有集中性'的数组元素里面抽取，指标实体名称在'业务小类维度，指标：'后面，例如以下数组元素：\n    '业务小类维度，指标：TCP建链平均时延(ms)，无集中性，集中度为0.5000，低于阈值0.5000'\n    这里面'TCP建链平均时延(ms)'中'TCP建链平均时延'就是指标实体名称,,最右边'(ms)'是单位，其他指标也一样，切记不要附加到指标名称后面\n  \"#) \n  @@stream.done\n}\nclass Bus {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何指标实体id重复\n  \"#) \n  n string @description(#\"\n    业务小类实体名称，如果名称相同则为同一实体，这点非常重要，从$data.outputs.report.results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'业务小类维度'开头和包含'有集中性'的数组元素里面抽取，查找'、指标值'前面的业务小类实体名称，例如以下数组元素：\n      '业务小类维度，指标：TCP二三次握手平均时延(ms)，有集中性，集中度为0.9574，高于阈值0.6000\\n\\t苹果iCloud、指标值：129.07, 总时延：1936.09, 成功次数：15.00, 影响度：-0.0362\\n\\t73001、指标值：140.71, 总时延：5628.27, 成功次数：40.00'\n      这里面'苹果iCloud','73001'就是业务小类实体名称\n  \"#)  \n  @@stream.done\n}\n\nclass IndexAndBusRel {\n  s int @description(#\"\n    头部节点,设置为指标实体id\n  \"#)\n  t int @description(#\"\n    尾部节点id,设置为业务小类实体id\n  \"#)  \n  v string @description(#\"\n    指标值，$$data.outputs.report.results[*].data[*].row[*]..用户指标集中性分析[*]..业务小类维度..指标值里面抽取，例如：\n    '苹果iCloud、指标值：129.07, 总时延：1936.09, 成功次数：15.00, 影响度：-0.0362'\n    这里面129.07是指标值，1936.09是指标分子，15.00是指标分母\n  \"#)\n  j string @description(#\"\n    指标是否有集中性，$$data.outputs.report.results[*].data[*].row[*]..用户指标集中性分析[*]提取\"有集中性\"和\"无集中性\"，有集中性设置为1，无集中性设置为0\n  \"#)    \n  z string @description(#\"\n    指标分子\n  \"#)   \n  m string @description(#\"\n    指标分母\n  \"#)   \n  @@stream.done\n}\n \n\n\nfunction GenIndexYW(json:string,extract_type:string) -> IndexYW { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    要注意，以下几点非常重要，不遵循会导致致命错误：\n    1、所有实体的id必须全局唯一\n    2、遇到相同指标合并为一个指标实体\n    3、遇到相同业务小类合并为一个业务小类实体\n    #extract_type:{{extract_type}}    \n    {{IndexList()}}\n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestIndexYW {\n  functions [GenIndexYW]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n    extract_type #\"indexOfBus\"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\IndexZD.baml": "class IndexZD{\n  extract_type string @description(#\"\n    设置为'#extract_type'的值\n  \"#)  \n  task_id string @description(#\"\n    案例id，从$task_id获取\n  \"#) \n\n  indexOfTerm IndexOfTerm[]  @description(#\"\n    指标实体集合\n  \"#)    \n  terminal Terminal[] @description(#\"\n    终端实体集合\n  \"#) \n  links IndexAndTermRel[] @description(#\"\n    指标实体和终端实体关系集合，从$data.outputs.report.results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'终端维度'开头的数组元素里面抽取，注意要在同一个数组元素里面判断指标实体和终端实体的关系\n  \"#)    \n}\n \nclass IndexOfTerm {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何终端实体id重复\n  \"#)   \n  n string @description(#\"\n    指标实体名称，如果名称相同则为同一实体，这点非常重要，从$results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'终端维度'开头和包含'有集中性'的数组元素里面抽取，指标实体名称在'终端维度，指标：'后面，例如以下数组元素：\n    '终端维度，指标：TCP建链平均时延(ms)，有集中性，集中度为0.5000，低于阈值0.5000'\n    这里面'TCP建链平均时延(ms)'中'TCP建链平均时延'就是指标实体名称,,最右边'(ms)'是单位，其他指标也一样，切记不要附加到指标名称后面\n  \"#) \n  @@stream.done\n}\nclass Terminal {\n  i int @description(#\"\n    设置一个全局唯一整数id,不能和任何指标实体id重复\n  \"#) \n  n string @description(#\"\n    终端实体名称，如果名称相同则为同一实体，这点非常重要，从$results[*].data[*].row[*]..用户指标集中性分析[*]数组中抽取，并且必须在以'终端维度'开头和包含'有集中性'的数组元素里面抽取，查找'、指标值'前面的网元实体名称，例如以下数组元素：\n    '终端维度，指标：TCP建链平均时延(ms)，无集中性，集中度为0.5000，低于阈值0.5000\\n\\t苹果_A3104、指标值：205.03, 总时延：6356.00, 成功次数：31.00, 影响度：-0.0788'\n    这里面'苹果_A3104'就是终端实体名称\n  \"#)  \n  @@stream.done\n  \n}\n\nclass IndexAndTermRel {\n  s int @description(#\"\n    头部节点,设置为指标实体id\n  \"#)\n  t int @description(#\"\n    尾部节点id,设置为终端实体id\n  \"#)    \n  v string @description(#\"\n    指标值，$results[*].data[*].row[*]..用户指标集中性分析[*]..终端名称后面的'指标值:'里面抽取，例如：\n    '苹果_A3104、指标值：205.03, 总时延：6356.00, 成功次数：31.00, 影响度：-0.0788'\n    这里面205.03是指标值，6356.00是指标分子，31.00是指标分母\n  \"#)\n  j string @description(#\"\n    指标是否有集中性，$results[*].data[*].row[*]..用户指标集中性分析[*]..提取终端维度的指标有无集中性，'有集中性'设置为1，'无集中性'设置为0\n  \"#)    \n  z string @description(#\"\n    指标分子\n  \"#)   \n  m string @description(#\"\n    指标分母\n  \"#)   \n  @@stream.done\n}\n \n\n\nfunction GenIndexZD(json:string,extract_type:string) -> IndexZD { \n  client Hs_deepseek_v3_250324\n  // client Hs_deepseek_r1\n  // client QwenCodeV2_5\n\n  prompt #\"    \n    你是一个JSON结构和图数据分析专家，擅长从JSON结构中抽取实体和实体关系信息。\n    我会提供详细的JSON结构给你，你需要仔细分析JSON的结构，按照我要求的输出格式生成一个图数据结构。\n    要注意，以下几点非常重要，不遵循会导致致命错误：\n    1、所有实体的id必须全局唯一\n    2、遇到相同指标合并为一个指标实体\n    3、遇到相同终端合并为一个终端实体\n     #extract_type:{{extract_type}}    \n    {{IndexList()}}\n    ###JSON###\n    {{json}}\n    {{ ctx.output_format }}\n  \"# \n}\n\ntest TestIndexZD {\n  functions [GenIndexZD]\n  args {\n    json #\"\n        统计开发人员的缺陷数量并且列出开发人员的详细信息  \n    \"#\n    extract_type #\"indexOfTerm\"#\n  }\n}  ",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\clients.baml": "client<llm> Hs_deepseek_r1 {\n  provider openai-generic\n  options {\n    model \"deepseek-r1-250120\"\n    base_url \"https://ark.cn-beijing.volces.com/api/v3\"\n    api_key \"8e8723a1-8032-4213-9752-748fbd9fbd70\"\n  }\n  \n}\n\nclient<llm> Hs_deepseek_v3_250324 {\n  provider openai-generic\n  options {\n    model \"deepseek-v3-250324\"\n    base_url \"https://ark.cn-beijing.volces.com/api/v3\"\n    api_key \"8e8723a1-8032-4213-9752-748fbd9fbd70\"\n  }\n  \n}\n\nclient<llm> Hs_deepseek_v3 {\n  provider openai-generic\n  options {\n    model \"deepseek-v3-241226\"\n    base_url \"https://ark.cn-beijing.volces.com/api/v3\"\n    api_key \"8e8723a1-8032-4213-9752-748fbd9fbd70\"\n  }\n  \n}\n\nclient<llm> DeepSeek_Qwen32 {\n  provider openai-generic\n  options {\n    model \"deepseek-ai/DeepSeek-R1-Distill-Qwen-32B\"\n    base_url \"https://api.siliconflow.cn/v1\"\n    api_key \"sk-dkihzhdvaowvyastvjtnhutiyyfrsntqnptbpehuvbhfytzz\"\n  }\n}\n\n\n\nclient<llm> DeepSeekV3 {\n  provider openai-generic\n  options {\n    model \"deepseek-ai/DeepSeek-V3\"\n    base_url \"https://api.siliconflow.cn/v1\"\n    api_key \"sk-yfibapxotobrduqertxcvhpjykczzjikeekxcrrgsqspggkd\"\n  }\n}\n\nclient<llm> QwenCodeV2_5 {\n  provider openai-generic\n  options {\n    model \"qwen2.5-coder-32b-instruct\"\n    base_url \"https://dashscope.aliyuncs.com/compatible-mode/v1\"\n    api_key \"sk-8cab883d8d964e0281cf774a552f98b4\"\n  }\n  \n}",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\main.baml": "generator lang_typescript {\n  output_type \"python/pydantic\"\n  output_dir \"../\"\n  version \"0.83.0\"\n  default_client_mode \"sync\"\n}\n\n\nclass DynamicOutput {\n  @@dynamic\n} \n\n\n",
    "../c:\\Workspaces\\Codes\\develop\\mutil-agent\\chatdata\\data-query-engine\\pandas-ai\\baml_src\\public.baml": "template_string IndexList() #\"\n注意：在抽取指标实体时，包括有但不限于以下所列：[\"附着请求次数\",\"附着成功率\",\"附着平均时延\",\"注册请求次数\",\"注册成功率\",\"注册平均时延\",\"业务请求次数\",\"业务请求成功率\",\"业务请求成时延\",\"上下文配置请求次数\",\"上下文配置成功率\",\"上下文配置时延\",\"寻呼请求次数\",\"寻呼响应成功率\",\"寻呼响应平均时延\",\"DNS解析请求次数\",\"DNS解析成功次数\",\"DNS解析总时延\",\"DNS解析成功率\",\"DNS解析平均时延\",\"TCP建链请求次数\",\"TCP建链成功次数\",\"TCP建链总时延\",\"TCP建链成功率\",\"TCP建链平均时延\",\"TCP建链请求次数\",\"TCP一二次握手成功率\",\"TCP一二次握手平均时延\",\"TCP二三次握手成功次数\",\"TCP二三次握手成功率\",\"TCP二三次握手平均时延\",\"PDU请求次数\",\"PDU成功次数\",\"PDU建立成功率\",\"PDU建立平均时延\",\"PDU资源配置请求次数\",\"PDU资源配置成功次数\",\"PDU资源配置成功率\",\"PDU资源配置平均时延\",\"PDN连接请求次数\",\"PDN连接成功次数\",\"PDN连接成功率\",\"PDN连接平均时延\",\"默认承载激活次数\",\"默认承载激活成功次数\",\"默认承载激活成功率\",\"默认承载激活平均时延\",\"专用承载激活次数\",\"专用承载激活成功次数\",\"专用承载激活成功率\",\"专用承载激活平均时延\",\"eps承载资源分配次数\",\"eps承载资源分配成功次数\",\"eps承载资源分配成功率\",\"eps承载资源分配平均时延\",\"E-RAB配置请求次数\",\"E-RAB配置请求成功次数\",\"E-RAB配置成功率\",\"E-RAB配置平均时延\",\"切换次数\",\"成功次数\",\"成功率\",\"平均时延\",\"TAU请求次数\",\"TAU成功次数\",\"TAU成功率\",\"TAU平均时延\",\"移动性注册请求次数\",\"移动性注册成功率\",\"移动性注册平均时延\",\"HTTP请求次数\",\"HTTP响应成功次数\",\"HTTP响应成功率\",\"HTTP响应平均时长\",\"HTTP大包下载流量\",\"HTTP大包下载时长\",\"HTTP大包下载速率\",\"HTTP掉线率\",\"TCP会话数\",\"TCPIP包数\",\"TCP上行IP包数\",\"TCP下行IP包数\",\"TCP重传率\",\"TCP上行重传率\",\"TCP下行重传率\",\"TCP丢包率\",\"TCP上行丢包率\",\"TCP下行丢包率\",\"上行乱序率\",\"下行乱序率\",\"上行RTT\",\"下行RTT\",\"会话数(HTTPS)\",\"上行流量(HTTPS)\",\"下行流量(HTTPS)\",\"上行IP包数\",\"下行IP包数(HTTPS)\",\"上行乱序率(HTTPS)\",\"下行乱序率(HTTPS)\",\"TCP上行重传率(HTTPS)\",\"TCP下行重传率(HTTPS)\",\"上行RTT(HTTPS)\",\"下行RTT(HTTPS)\",\"TLS握手请求时延(HTTPS)\",\"TLS握手响应时延(HTTPS)\",\"第一个应用数据时延(HTTPS)\",\"TLS请求超时率(HTTPS)\",\"TLS响应超时率(HTTPS)\"]\n\"#",
}

def get_baml_files():
    return file_map